extends layout

block title
    title= 'Web server behind HA Proxy'
    div(class='center-div')
        table
            tr(class='bottom-margin')
                th Requesting IP address
                th ip
            tr(class='bottom-margin')
                th HA Proxy server address
                th ip
            tr(class='bottom-margin')
                th Server IP address
                th ip
            tr(class='bottom-margin')
                th header info
                th info